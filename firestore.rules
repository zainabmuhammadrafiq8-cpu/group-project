rules_version = '2';

service cloud.firestore {

  match /databases/{database}/documents {

    // Logged-in users ke liye
    match /{document=**} {

      allow read, write: if request.auth != null;

    }

  }

}